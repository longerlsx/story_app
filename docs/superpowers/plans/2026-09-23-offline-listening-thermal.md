# ZipVoice 长听发热诊断与改善

日期：2026-09-23。状态：候选D（四线程／三步／完整参考）首次1.45×真机20分钟温度读数达标、声音连续，用户体感减轻；电荷计有46mAh延迟更新，至少15%节电及方向重复尚未达证。手机保留待复测D，最新设置／进度已恢复并交还。此前三线程候选C已撤回；不宣称目标完成。[调查记录](../../knowledge/bugs/BUG-2026-062-zipvoice-long-listening-thermal.json)。

## 范围与证据门槛

主类型：性能改善；次类型：与浪费计算直接相关的可靠性修复。主结论必须来自代表性小米真机长听前后对照，不能由模拟器、线程数或单句耗时推出。

保留免费、完全离线、内置雷军音色、用户选择的实际语速；正文、书籍、设置和进度不丢。用户在9月23日下午明确允许暂不支持2×，以实际发热的1.45×为主验收；这是取舍授权，不是认可三线程或任何路线。若只在部分语速有效，必须明确条件和未支持范围，不能宣称全速改善，不能静默降速。温升、耗电与连续播放门槛不放宽；用户后来允许轻微听感损失换取效率，具体声音取舍通过实际样本确认，不将漏读或听不清默认归入可接受损失。暂停／停止／定时／通知／焦点恢复继续正确。继承9月22日未受影响的回归，不重新展开音源选型或全项目调查。

一次性验收代码预算：预计0–100行，硬上限800行；默认不进入仓库。全队合计，临时源码、配置、原始采样放 `/private/tmp/story-thermal-20260923`，结束清理。复用现有日志、ADB、实际录音及测试；不建benchmark或验收平台。

**当前授权边界：**此前各约定窗口均完成并恢复当次最新设置／进度。最新真机窗口后手机保留三步D，显示设置恢复、无线ADB关闭并交还，不是仍留原四步包。用户现反馈两步较长声音不清楚，只要求制作“三步＋短参考”同文试听，备妥后暂停目标等回复；不继续扩展优化、自动安装或安排真机。下一次真机仍须约好窗口、明确开始并新备份，不自行续占手机或用旧备份覆盖后续进度。

## 基线与当前事实

- 工作区起始干净，分支 `feat-android-reader-foundation`，HEAD `ac83a5c`，与已知远端跟踪一致。产品代码来自 `55b385a`；9月22日[实施记录](2026-09-22-zipvoice-listening-upgrade.md)和[声音依赖](../../knowledge/offline-voice-dependencies.md)继续有效，但不证明真机长听。
- 本轮测量设备：小米15 Ultra，型号25019PNF3C、Android16。问题APK与保留的9月22日交付包SHA256均为 `f65e016f24fb8f0b7c82a9d8c65a1594e3228f23ea0597dc19e995f3bbcaba78`。该版本基线已取得并保留，后续覆盖候选均不清数据。
- 首次读数USB供电、电量45%、电池30.1°C、HAL当前skin31.0°C、thermal status0。`dumpsys thermalservice` 的Cached temperatures是历史缓存，不能当作当前温度；读取Current temperatures from HAL。skin是系统传感器／估计量，不等同用户触感或外壳实测。
- 用户已补充：最初反馈的是17 Pro、1.45×、亮屏、不充电，明显快速升温且掉电快，具体多久不确定；17 Pro未带来。随后15 Ultra在约好的20分钟窗口内也复现明显发烫，详见基线A；其结果不直接证明17 Pro改善。手机操作干扰的样本排除，后续真机窗口受上面的重新确认边界约束。

## 执行顺序与停止线

1. 校准可读传感器、进程CPU、实际音频和生成日志；备份原书籍／设置／进度，固定代表正文起点。分别核对持续播放、暂停和停止，判断主要时间花在必要生成还是额外负担。两项只读调查并行：引擎／预生成与服务／界面；主代理独占设备、构建和测量。
2. 固定手机、正文、语速、屏幕、音频路由、供电、相近初温及环境，取得达到用户发热时长且趋势可解释的稳定基线。尽量无线ADB拔线；若只能USB，明确能耗及充电影响限制。
3. **基线取得后、优化结果出现前**，登记本轮温升／耗电改善标准及允许波动；已冻结在下方，不按候选结果放宽。基线已复现，但20分钟尚未达到完全热稳态。
4. 按证据只处理最可能有实际收益的成本，每轮明确会改变决策的问题。用户后续允许将有依据的方法组合，不要求精确拆分各项贡献；组合仍须验收最终温耗与体验。需要行为改动先加直接回归；复杂调度方案先独立检查，实质并发／生命周期改动完成后做一次有边界实现检查，不递归审查。
5. 相同条件做优化前后长听，热态检查生成余量／断流／漏读／重复、首声与响应、暂停／停止／熄屏；实际声音用于连续性，回调只说明状态。必要复测用于区分收益与波动，不无限调参。
6. 达到预定标准且关联回归和检查问题收口后交付覆盖安装APK、代码、简洁结果与限制；分独立成果及时提交推送。若有依据尝试仍不达标，记录剩余成本、排除路线及经小样本验证的取舍，保持目标未完成身份。

## 最小直接证据

| 问题 | 入口与决策 |
| --- | --- |
| 必要生成还是额外负担 | 已有synthesisStart／synthesisMs日志＋进程／线程CPU；不生成期间仍持续繁忙才追空转，重叠／重复源偏移才追队列 |
| 暂停／停止有无无效工作 | 正常UI／通知控制＋日志／CPU与音轨，分清不可抢占单次ONNX尾部和持续新任务 |
| 长听温升与耗电 | 当前HAL、电池温度／charge counter及实供电状态；无可靠能耗则不报耗电下降比例 |
| 连续供给与控制是否退化 | 同正文／语速真实输出录音、源区间与生成余量，复用已有正式引擎／后台回归 |
| 覆盖安装保护数据 | 安装前后原书籍／设置／进度核对，不清应用数据，不以新建样本替代升级 |

详细原始日志不入docs；后续在此维护条件、标准、结果和决策摘要，当前结论只在current-state汇总。

## 诊断记录

- 15 Ultra问题包正常UI，当前《咸鱼和反派错绑对方系统后》第5章，原设置1.3×、不定时；约一分钟生成样本每段29–40字、合成2.218–3.504秒、自然音频5.312–7.754秒，源偏移按顺序推进，未见正常连续重复。样本仅校准日志及CPU，不是1.45×长听基线；USB供电，未录音，不声称声音连续性通过。
- 生成时线程CPU主要集中于原生计算所在线程及其工作线程；主动暂停后5秒采样窗口总CPU依次约49%、0%、0%、0%、0.8%、0%（100%=一个CPU核），且没有后续synthesisStart、唤醒锁数0。停止后服务和媒体会话消失，进程CPU回到0%。原生单次调用取消尾部仍在，未发现暂停后无限计算。
- 两项独立只读调查均未发现无界预生成或应用层忙循环；预备限后两段，倍速不使自然PCM失效。定时每秒重建通知是已识别额外工作，但当前未设定时，且成本未知，不优先扩展此路线。原生推理阶段间短时自旋仍待测，不能由暂停最终0%排除或认定其贡献。
- 现有`ReaderOfflineListeningTest`、`ReaderTtsBackgroundPlaybackTest`会在setup/teardown重置用户数据，只能用于模拟器；真机走正常UI与外部读数。原书籍／设置／进度已临时备份。短诊断结束已正常停止、恢复1.3×，未覆盖安装。

## 问题包长听基线A（修改前）

2026-09-23 11:03开始，15 Ultra／原APK，Wi-Fi ADB，实际USB／AC／无线供电均false；当前小说《咸鱼和反派错绑对方系统后》，源SHA256 `c08f113a801d59217117bb2e4a27ccb997a07aa75d6646766aad07016b21a524`，chapterIndex=3／源偏移987开始，1.45×、不定时、亮屏日间主题。临时固定系统亮度41、自动亮度关闭、息屏30分钟；原值60000ms／自动亮度1／亮度41已保存。室温未独立测量，物理扬声器静音；scrcpy 4.1以playback内部音频捕获，持续录制、不投屏、不外放。其CPU快照约4–5%，后续保持同样采集负担。普通后台应用仍在，不能视作实验室隔离。

固定20分钟窗口：11:03:00（epoch1790132580）至11:23:00；30秒采样，末个窗口内样本在19分33秒，另在20分26秒补结束读数。温度尚缓慢上升，**不是完全热稳态**。用户在测试结束后确认外壳已经明显发烫；这是体感证据，与HAL skin估计值分开。

| 指标 | 问题包基线 |
| --- | --- |
| 电池温度 | 31.6→42.7°C；结束补读仍42.7°C |
| 当前HAL skin | 32.756→46.406°C（窗口内末样本）；第15–20分钟均值45.853°C |
| 电荷计数 | 2286→1830mAh，净减少456mAh；电量45→36%。计数间歇更新，末次跳变量达约36mAh，不当作高精度功率计 |
| 冷／热生成余量 | 前5分钟67段合成中位2745ms，对应1.45×供给比中位0.605；最后5分钟68段3737.5ms／0.857，合计计算／对应播放时间比0.873。不同正文存在影响，不能把全部差值称作降频比例 |
| 实际声音 | 首声约5.8秒；窗口内检出一次≥0.6秒静音，约13分18秒处1.765秒，对应短段过渡后下一段生成尚未完成；其余该窗口未检出≥0.6秒静音 |
| 重复／漏读 | 源段按序，无相同章／偏移重复生成；冷态和热态各35秒独立ASR均已与原文核对，未见整句漏读／重复，专名同音不算缺陷；结论不推广到任意文本 |

20分钟后另作20秒simpleperf（99Hz、task-clock:u、DWARF），7729样本、0丢样，约98%以上用户态CPU样本在libonnxruntime.so；内部符号为地址，不能直接分离矩阵计算与自旋占比。该采样及报告不计入温升窗口。服务／界面不是本样本主要CPU来源。无依据先改定时通知、参考特征缓存或GPU。

随后正常UI切2×约131秒（epoch1790133884–1790134015）。真实音轨出现多次约0.6–3秒静音，最长2.950秒，确认热态供给不足；前置profiling有短暂额外负载，不能单据此量化2×温升，但其声音停顿真实存在。完成后停止服务、关闭采样、恢复原1.3×及系统显示设置；保存测试后位置证据，再恢复测试前阅读／听书位置，当前书chapterIndex=3／charOffset866，书籍未清除、设置未重置。手机已交还用户使用。

## 修改前冻结的本轮通过标准

以下在任何优化候选构建／测量前确定，不按结果放宽：

- 代表窗口为同一15 Ultra、同书同源起点987、1.45×、20分钟亮屏不充电、同固定亮度与录音方式；初始电池31.6±0.5°C、skin32.756±0.7°C。记录电量、供电、前台及后台干扰，明显被用户操作或供电改变的窗口不比较。室温未测的限制保留。
- 温升：第15–20分钟skin均值至少比45.853°C低1.5°C，20分钟附近电池温度至少比42.7°C低1.0°C；再取得一次同方向的有效重复长听，不能用短样本替代。
- 耗电：相同窗口电荷净减少至少下降15%（≤387.6mAh）。允许计数滞后／后台差异约36mAh的观察波动；若差异不能可靠超过读数波动，明确耗电未达证，不报精确节电比例。温度和耗电均须成立，CPU减少本身不算通过。
- 播放：1.45×不增加供给性长静音，力争消除已有1.765秒过渡；不允许以降速或频繁等待换低温。首声不增加超过1秒，暂停／停止响应仍及时，无取消后的持续新合成；焦点、定时、熄屏及进度的直接受影响路径补验，未改路径继承旧证据。原先要求热态2×供得上，用户在候选C的1.45×长测前明确取消这一必过项；2×实测不足保留为已知限制，不再阻断1.45×温度／电荷验收。
- 若有限实验不能同时达到标准，记录未达标、剩余成本和经验证取舍，不把“实现候选”当成目标完成。

## 首轮候选筛选（真机长测前）

- 首个候选完整关闭ORT intra／inter-op自旋，保持四线程／四步。模拟器真实音频检出1.886秒供给静音；匹配测试的原APK也出现首段后0.909秒、短尾后3.333秒静音，对应35字片段生成耗时6954ms异常。两者均有供给风险／环境波动，不能把候选停顿归因于关闭自旋，也不能宣布纯性能回退；现有证据不足以选择全关自旋，不作为首选。
- 候选B只设置 `SessionConfig.session.force_spinning_stop=1`，保留四线程／四步、完整雷军参考音频及现有正文分段。该项使会话最后一个并发 `Run()` 返回时立即停止内部线程池自旋，下次 `Run()` 重新启用其原有等待策略；不完整关闭运行期间自旋。同三文本累计进程CPU两轮分别为原版33811／39717ms、候选38728／32079ms，方向相反，未显示稳定省CPU，不进入真机长测。模拟器CPU及声音仅用于筛选，不能证明手机温升或耗电改善。
- 迁移依据：[sherpa v1.13.8 session.cc](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/session.cc)支持 `cpu:配置文件` 并转发 `SessionConfig.` 键；[ZipVoice模型](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/offline-tts-zipvoice-model.cc)及[生成实现](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/offline-tts-zipvoice-impl.h)分阶段调用Encoder、Decoder和Vocos，同一配置可传至这些会话。[ORT配置说明](https://github.com/microsoft/onnxruntime/blob/v1.28.2/include/onnxruntime/core/session/onnxruntime_session_options_config_keys.h#L251-L256)给出 `force_spinning_stop` 的语义；这支持测试会话间等待成本，不证明其实际占比或收益。
- 保留完整参考的长段微样本：同文34字＋36字分段两轮累计CPU为19167／21919ms，合并为70字后为20580／25817ms；合并首声5736／7538ms，原首小段2757／2856ms。保持正常标点和 `min_char_in_sentence=30`，未证明固定成本摊薄收益，不改产品分段；不能仅按字数宣称合并更省电。
- 候选C仅将四线程改为三线程，保持完整参考、四步、默认自旋、40字上限及两段预备。同三文本两轮累计进程CPU为27221／24584ms，均低于四线程33811／39717ms；六样本合成墙钟总时间18.135秒，对照21.057秒，生成自然音频总长41.233秒／41.407秒。结果支持保留C作为待真机验收候选，不代表手机节电约30%或温升改善。短音频和控制回归结果见下方收口记录，不遍历更多线程数。

## 有边界横向研究

| 项目与官方来源 | 供音、队列与后台策略 | 对本App的意义 |
| --- | --- | --- |
| Legado：[系统TTS](https://github.com/LegadoTeam/legado/blob/master/app/src/main/java/io/legado/app/service/TTSReadAloudService.kt)、[HTTP供音](https://github.com/LegadoTeam/legado/blob/master/app/src/main/java/io/legado/app/service/HttpReadAloudService.kt)、[服务控制](https://github.com/LegadoTeam/legado/blob/master/app/src/main/java/io/legado/app/service/BaseReadAloudService.kt) | 系统路径调用外部Android TTS引擎，按章排队，暂停调用stop；HTTP路径下载外部音频，队列上限12、按文本文件复用并有128MiB流缓存，暂停保留播放器队列且未立即取消在途下载。前台服务、媒体会话与音频焦点负责后台控制。 | 有界队列和结果复用可参考；HTTP等待策略不能直接套给高成本本地生成，整章系统队列也不是本App的目标。 |
| wallabag Android：[TtsService](https://github.com/wallabag/android-app/blob/master/app/src/main/java/fr/gaulupeau/apps/Poche/tts/TtsService.java) | 调用默认或所选Android TTS引擎；当前项加后两项，完成回调再补充，代次ID忽略旧回调。暂停保存位置并stop，恢复重建队列；前台通知、媒体会话与焦点管理后台朗读，服务层未实现音频文件缓存。 | 有界预备及旧回调失效可借鉴，本App已有相应机制；仅为换框架而重写没有证据支持。 |

两者均未提供此处可用的同机同音色功耗对照，实际系统TTS是否离线取决于外部引擎；都不能直接满足本App内置雷军克隆、无需额外安装／下载的约束。云端供音把计算移出手机，不能作为公平热量对照。值得保留的机制仅为有界预备与可复用已完成音频；本App前者已具备，后者只有实际重复请求时才可能节省生成，不能据此盲目扩大缓存，也不能降低首次连续长听的必要计算成本。

官方ZipVoice另建议参考录音短于3秒以加快推理（[使用建议](https://github.com/k2-fsa/ZipVoice#3-guidance-for-better-usage)）；当前6.057秒参考会进入每步Decoder，裁短有计算依据，但声学条件随之改变，不能只保留雷军标签就认定听感相同。首轮未裁参考、改模型或减步；候选C撤回后的桌面小样本研究见末节，产品仍使用完整参考。长段方案还会扩大暂停重播与跟读粒度，首个短段未必能供给下一长段；独立方案检查支持暂不扩大App分段，旧微样本未真正减少native块的限制见后文。系统TTS、HTTP供音、GPU及重编推理库没有满足当前全部约束且已验证的收益，暂不扩展。

累计CPU来自既有 `ReaderOfflineVoiceTest` 增加的 `Process.getElapsedCpuTime()` 日志：初始化完成后、提交朗读前至实际播放完成，含App内测试轮询、回调等所有线程，不含初始化／shutdown及其他进程；线程累计时间可大于墙钟。三段样本为现有默认34字叙述加36字对话、37字叙述，每版本各两轮，2×播放；样本少且模拟器受宿主调度影响，只用于筛选。

## 候选C电脑准备与首次暂停（历史）

- 产品改动仅为 `OfflineReaderTtsEngine` 的线程数4→3及对应诊断日志；不保留自旋配置试验，不改资源、四步、自然合成／播放器倍速、分段、队列和生命周期。既有实声测试入口补累计进程CPU日志。独立实现检查未发现代码正确性或生命周期问题，并明确CPU读数不是功率、供给仍须测。
- 最终相关构建 `assembleDebug` 成功；测试APK增加CPU日志后的 `assembleDebugAndroidTest` 成功，未再修改测试。三线程六个单段实引擎测试通过；同一候选三项 `ReaderOfflineListeningTest` 共121.1秒通过：2×暂停／继续／停止／页顶重开并保留定时选择，熄屏跨章与通知控制，合成中失焦保留已生成音频且归还焦点才播放。没有为纯线程配置重复跑未受影响的30分钟定时、导入或分页套件，继承旧证据。
- scrcpy实际播放录音中，2×从首声至主动暂停约9.15秒，没有≥0.6秒静音；完整读出首章及随后已播放正文，独立本地ASR与测试正文对应。其余长静音对应主动暂停、停止、测试切换或等待归还焦点，不算供给停顿。范围很短，不能外推任意正文、热2×或长听。该次UI点击至首声9.206秒，对照原APK一次7.618秒，存在约1.59秒差异；同文单段多次样本并未整体变慢，但不能将这一次首声差异忽略，真机须按原先≤1秒退化门槛重验。
- [候选APK](/Users/longshengxi/proj/story_app/app/build/outputs/thermal-investigation/daliamao-reader-thermal-candidate-20260923.apk)：224,987,376字节，SHA256 `0981cda1fdc1204d4484cf9368616408dd7932e79958af2f7e8eb1645dba3c5b`，仅ARM64、debug、包名与版本号沿用。签名校验通过，证书SHA256与问题包同为 `cba64d3b44548055d79ff4e677e64d7f464cdf59fbaf538cf41a377dfc1912eb`；366项assets／原生库的大小与CRC均相同。首次暂停时仅模拟器覆盖安装；后续真机安装见下节，始终未清数据。
- 为续接比较，原始基线音轨、必要温度／电荷／生成日志及候选筛选证据保存在本机已有忽略规则覆盖的 `app/build/outputs/thermal-investigation/evidence/`，不入Git。9月22日问题APK原位保留。本轮 `/private/tmp/story-thermal-20260923` 已清理，包含临时脚本、下载工具／ASR模型、原书备份及中间输出；任务模拟器已关闭。临时脚本实际64行，连同已执行的内联采样解析等保守估计不超过220行，超过最初预计但低于全任务800行硬上限；续接仍累计计算，不重新获得预算。

首次在此按用户要求暂停目标，等待重新约定窗口，不报告“发烫已解决”或节电百分比。该次遗留手机profiling临时文件与无线ADB已在下述授权窗口清理。新的1.45×实测依据用户最新范围推进，既定温度、电荷和1.45×播放门槛不放宽。

## 真机短测与底层成本补证

14:18–14:40授权窗口：先保存手机当时最新书籍、设置和进度，覆盖安装同一候选C；无线ADB连通后用户明确拔USB。2×短测约105秒（epoch1790144840.287–1790144945.281），与基线同章、源起点987及亮屏条件，初始电池34.9°C高于规定范围，**不是温度／耗电对照**。实际录音有1.129、2.339、1.162秒供给停顿；30个完成段合成102.716秒、自然音频190.860秒，累计合成／2×播放时长比1.076（含首段生成，非纯稳态比）。因此未进入当时仍要求2×通过的长测；用户随后允许2×暂不支持，C应保留为1.45×待验候选，不能根据这次短测判定1.45×失败或收益成立。

使用现有AAR支持的 `ProfilingFilePrefix` 临时启用ORT记录，正常UI按1.45×运行约54.56秒后停止。独立解析三个有效JSON，按 `model_run` 墙钟汇总如下；每个Decoder Run是一生成步。

| 阶段 | Run次数 | 总墙钟 | 已记录阶段占比 | 单Run中位／P90 |
| --- | ---: | ---: | ---: | ---: |
| 文本Encoder | 14 | 0.315秒 | 0.68% | 23.7／25.1ms |
| 四步Decoder | 56 | 43.643秒 | 94.80% | 794.7／885.5ms |
| Vocos ONNX | 14 | 2.080秒 | 4.52% | 145.0／176.7ms |

14次App生成启动均对应1 Encoder＋4 Decoder＋1 Vocos，样本未覆盖双层短尾。13次记录完成，末段原生尾部约在停止标记后2.04秒结束，不代表仍播放或提交进度，也不是持续新生成。以上含profiling开销，排除了模型加载、参考预处理、Run外向量更新与原生IStft；**不是CPU或能耗占比，未分离同步等待**。结论仅支持优先检查Decoder，不支持擅自减步、改音色或按95%推断节电潜力。[固定版本启用入口](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/session.cc#L166-L170)、[模型分阶段实现](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/offline-tts-zipvoice-model.cc)。临时profiling改动已移除，命名候选APK不含该探针。

对原simpleperf样本补做指令辨认：相同运行库内确认UMMLA计算指令子集622样本（8.05%），yield／原子读取等待子集246样本（3.18%）。仅为已识别地址子集，不能推广成完整计算／空转比例或认定等待都可消除。固定参考缓存可避免参考文字与Mel特征前处理，但[官方缓存路径](https://github.com/k2-fsa/ZipVoice/blob/master/runtime/nvidia_triton/pytriton_server.py#L78-L97)仍执行生成和声码器；当前证据不支持优先重编原生库做缓存或共享线程池。

现有ORT JSON虽含主线程Run／Wait字段，工作线程 `num_run` 却包含跨节点累计活动，没有各工作线程CPU、自旋或睡眠时间。该补查不能将Wait等同可消除的浪费，也不能补出完整忙等占比；不据此开启新的原生审计。

双层分段核对修正了旧微样本的解释：34字＋36字合为70字调用后，底层仍分为34＋36，故该实验没有检验减少模型生成块的收益。基线重建275个App段对应306个原生文本块，其中31个追加短尾，16个有实际文字、15个只有闭引号；尚未确认纯标点尾是否被前端跳过，不把31个文本块当作31次完整推理。窄候选可在保留App分段／进度边界下合并内部尾段，但仍须核对停顿、首声与不可抢占取消尾部，不直接扩长所有App段。[固定版本分句合并](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/text-utils.cc#L1244-L1286)、[逐块生成](https://github.com/k2-fsa/sherpa-onnx/blob/v1.13.8/sherpa-onnx/csrc/offline-tts-zipvoice-impl.h#L152-L215)。

窗口末已恢复9月22日原包（手机APK SHA核对一致）及该窗口开始的新备份，三份设置／位置文件恢复时SHA相同，其他小说后续进度保留；恢复自动亮度与60秒息屏，正常开回小说。服务已停止，手机临时profile删除，无线ADB转回USB后交还用户。

## 1.45×对照安排（长测前决定）

用户明确2×可暂不支持后，先测已有C而不继续换参数：相同自然语速合成输入不随播放倍率改变，高倍速只增加单位时间供给需求；较低倍率可能留出更多空闲，但不能由此推断更少线程更省电。C若在1.45×达到既定温升／电荷标准，再取得同方向重复并检查相关控制；若只在部分倍率有效，交付须明确支持范围，保留高速案例另行研究，不能宣称全线改善。C若不达标，才以已识别Decoder成本及内部短尾为依据选择下一项有界实验。

本次新增临时分段复原40行、profiling解析25行，连同先前220行及本窗口内联操作／临时4行探针保守累计约390行，仍低于全任务800行硬上限；后续继续累计。原始证据保存在既有忽略目录，临时探针与备份按窗口结束清理，不进入Git。

## 候选C的1.45×窗口（未达标）

用户在等待冷却时明确指出不必拘泥精确初温，要求重点观察温度变化及耗电。据此在结果产生前改为记录实际初温直接运行，不延长等待；原先终温、后5分钟skin均值、电荷和播放改善目标不降低，不能只用较高起温下较小的温升幅度宣称通过。起温、SOC和未测室温差异须保留，结论结合整段曲线、终温与掉电，重复对照仍必需。

本轮于14:57:20（epoch1790146640.149）从同书chapter=3、offset=987开始，同一命名候选C、1.45×、亮屏、系统亮度41／应用0.8、不定时、拔线不充电及scrcpy 4.1实际音轨。电池服务初始33.3°C／3198mAh／63%，较旧基线31.6°C／45%偏高；完整传感器起点及后续读数保留于本轮采样。设置和位置已于本窗口独立新备份。熄屏冷却时一次ADB离线已恢复，发生在正式窗口前，不混入测量。20分钟结束自动媒体暂停。

窗口于15:17:20（epoch1790147840.349）结束，41次定期采样均成功，供电持续false、前台保持本App。起始skin33.937°C（旧32.756°C）；起温／SOC不同、室温未测，不将所有差异归因于线程数，也不以较小温升幅度宣称改善。

| 指标 | 四线程基线A | 三线程C | 原定改善目标 |
| --- | ---: | ---: | ---: |
| 电池起点→20分钟 | 31.6→42.7°C | 33.3→43.3°C | 终温≤41.7°C |
| 第15–20分钟skin均值 | 45.853°C | 46.228°C | ≤44.353°C |
| 20分钟附近skin | 46.406°C（19:33）；46.501°C（20:03） | 46.581°C（20:00） | 以上述时段均值判断 |
| 电荷减少 | 456mAh | 451mAh | ≤387.6mAh |
| 电量显示 | 45→36% | 63→54% | 不用整数电量替代电荷计数 |

电荷只差5mAh，远小于两轮约36–52mAh的间歇更新幅度，不能报告节电比例；温度也未达到目标。候选C撤回，生产默认恢复四线程；这不证明任何设备／语速下三线程必然无收益，而是本任务没有可接受的真机收益依据，不继续重复C长测。

暂停命令后约0.03秒AudioTrack进入暂停，5秒后至后续停止前进程仅增加约0.03秒CPU，未见持续新生成；正常媒体停止后服务消失。完整实录按−35dB／≥0.6秒检测，排除首声等待与末尾主动暂停后有24段内部静音，总32.060秒、最长2.716秒。独立核对最长及最后一个1.185秒静音，均对应前段AudioTrack已停、后段合成尚未完成，完成后立即播放，确认至少两处供给停顿；其余22段未逐段归因，不把总32.060秒一概称为供给缺口。原基线窗口仅一处1.765秒长静音，C的连续播放体验不满足要求。首段自然音频长度与原版相同；首声附近实际音轨与AudioTrack时间相符，点击至开播约6.4秒（旧约5.8秒），不是本轮主要退化。

20分钟内按章／源偏移匹配266个相同完成段，C全部更慢，配对耗时差中位+823ms、倍率中位1.242；累计生成891.752→1129.604秒，自然音频1671.066→1670.059秒。C末5分钟63个配对段的1.45×单段余量中位699→−223ms，负余量6→45段，总播放预算／生成时间1.168→0.922。原窗口完成274段、C完成266段，差别为尾部8段，未见重复完成键；源日志不能替代漏读音频检查。该真机方向与模拟器筛选相反，进一步支持撤回C；起温差仍限制严格因果归属。

手机已恢复原问题APK（SHA与留存包相同），本窗口开始时的设置／阅读／听书位置三文件恢复SHA均一致；自动亮度1、息屏60秒、服务为空均核对，正常开回小说，无线ADB转回USB后明确交还用户。不要再使用本窗口备份覆盖用户后续进度。此次C判定无需再跑无关旧回归或重复失败的长听。

三线程撤回后引擎文件与已验收四线程产品基线一致，沿用其有效回归，不为参数回退重跑整套。CPU测试日志仍保留。新增一次性采样28行、温度汇总约27行、同源最终配对累计25行、两次离线字段／声音解析20行及若干UI／恢复内联操作；连同前述全部阶段，保守累计约640行，后续仍受800行总硬上限约束。证据已归入既有忽略目录的 `device-stage-20260923` 与 `candidate-c1-20260923`，本轮临时探针、下载工具、模型profile原始展开及用户备份按结束清理。

## 下一项桌面小样本：同声参考长度

只在临时目录比较完整参考与其较短完整句，保留四线程、四步、模型、自然合成speed=1、分段及播放倍率，暂不改产品资源。假设是减少Decoder每步处理的参考帧；现有C++把参考tokens和特征帧F送入Encoder，Encoder输出总帧N，四步Decoder处理N，最后取N−F。因此裁短也会改变目标自然时长，不能由参考缩短比例推断收益，更不能用正文变慢换供给余量。

先对齐完整发音与参考文本，再用现有代表摘句、两次相反次序的同文小样本。筛选门槛预先定为累计CPU每秒有效自然音频至少减少15%、样本总自然时长变化不超过5%且不出现系统性变慢；首声、整句完整性、自然停顿和音色仍需直接声音检查。桌面结果只决定是否值得形成下一手机候选，不代替温度／电荷验收。若无合格样本，停止该路线并保留完整参考；本项试验不混入步数变化。若需可感知的声音取舍，先展示小样本与代价再由用户决定。

本地独立转写模型此前已按规范清理，本轮官方源和已知镜像的下载均在TLS阶段超时，不继续排查用户网络。先作不进入产品的计算可行性筛选：完整录音首个−35dB／50ms静音区间为1.875–1.981秒，暂以中间1.930秒作为“那还是三十六年前,”候选截点；这只是波形候选，不宣称发音边界已听验。仅当CPU和时长门槛有希望时，再补发音、音色与完整性确认，否则停止。完整参考文件始终不改。

该短参考ABBA同文累计CPU48.330→31.814秒（−34.2%），但原始音频39.893→42.667秒（+6.95%），最终静音压缩后39.036→40.170秒（+2.90%）。第二短参考轮及正文短尾实际播放分别增加约6.1%以上，不能用累计均值掩盖。按官方Encoder导出公式及稳定原始长度比，只补一次保音高节奏校准：首1.930秒经atempo=1.0695处理并定长1.80458秒，保持参考文本、speed=1和其他参数。校准后原始音频39.893→40.021秒（+0.32%），但最终39.893→37.166秒（−6.84%）；仍未通过原先±5%门槛，且没有音色／完整性听验。停止这份短参考候选，不继续调截点／节奏，不形成手机APK。计算收益是桌面CPU证据，不是电耗或改善交付。

## 底层短尾小样本（不作为主候选）

在本机相同sherpa 1.13.8、四线程／四步／完整参考下，现有三个摘句按30→100→100→30顺序比较原生合并阈值。35字正文加“何必呢”从两块变一块，平均CPU11.027→7.685秒，局部约少30.3%；普通34字控制仍一块，7.149→7.442秒。闭引号尾在原配置就被前端跳过，只有一块有效声音，日志明确OOV，不能算15次额外完整推理。

原长听275个App段仅16个有正文短尾（5.8%）。以本次普通段和短尾成本粗略加权，整体CPU机会约2–3%，不是手机耗电结果，没有支持本轮15%整体改善的依据，不为此单独占用下一长听窗口。它可作为局部优化备选；停顿韵律和取消检查点仍有代价，暂不改产品。Python绑定的extra必须整表赋值；最初逐项修改副本的试验作废，以上仅使用改正后的完整ABBA结果。独立取舍检查没有发现足以将其提升为主路线的证据。

## 完整参考的真正合块可行性

复用同一28行桌面入口，保留全部参考／四步／四线程，仅以34字叙述＋31字正文构成65字输入，按原生min30→100→100→30比较。它补查此前70字试验没有消除的实际模型边界，不代表已决定扩长产品分段。候选须同文累计CPU至少下降15%、最终时长变化不超过5%，才继续评估。首段仍短也不自动保证首声后连续：现有预生成从首段实际发声才启动，第二长段必须在首短段播放预算内完成。须由现有模拟器实声入口验证这一个接缝，再决定是否改变分段，不能为了摊薄参考而扩大预生成或牺牲取消响应。

该65字样本确实从两块变一块，累计CPU27.592→23.653秒（−14.3%），最终自然时长12.949→12.960秒。收益未达到本项15%筛选线，不挑有利单轮继续上手机。独立接缝检查还确认：加长段落会扩大手动暂停后重读边界、延后改速生效并拉长不可抢占计算尾部。没有足够理由为此改写进度或预生成框架，产品保持40字和原生min30。

## 三步生成声音取舍样本（用户允许继续验证）

前三项低风险路线未得到可接受主候选后，才以已观测Decoder成本为依据制作三步样本，不直接改手机。保持四线程、完整参考、模型、speed=1、silenceScale=0.2及min30，仅numSteps从4变3；这与已撤回的三线程不同。[官方列出的Distill最低配置为四步](https://github.com/k2-fsa/ZipVoice#32-speed-optimization)，三步低于该建议范围，不能仅凭能运行宣称音质保证。固定sherpa实现按num_steps循环执行Decoder，故该样本直接减少主要阶段的一次迭代；是否可接受须听实际声音。

三个摘句按4→3→3→4运行，四步累计CPU47.061秒、三步35.205秒（−25.2%）；最终音频39.732→39.893秒（+0.41%），回调原始长度相同，两轮三步均有同方向CPU下降。这里只是桌面计算筛选，不转换为手机节电比例。截取预定第一轮的前两个样本，统一用FFmpeg atempo=1.45制作约9秒同文对照，未挑选最好听的一轮；这不是Android AudioTrack实际播放录音。已展示给用户询问音色／清晰度／自然度，等待判断。没有独立转写或人工听验结论，不能据音频长度声称未漏读，也不能因为请求中允许2×暂不支持而推断用户接受音质变化。用户若认为明显退化，保留四步；若基本相当，再形成候选、复用既有相关回归并另约真机1.45×窗口。

此阶段原始JSON、必要声音样本保存在既有忽略目录 `evidence/desktop-decoder-20260923/`，不进入Git。临时生成入口28行、参考变体约31行及实质改写／数据汇总等连同此前阶段保守累计约770行，已接近全任务800行硬上限，停止新增验收工具；后续优先复用已有入口。临时源码及未采用声音在阶段结束清理。产品仍四线程／四步／完整参考，未操作已交还手机。

用户听完以上样本后明确回答“听感基本相当，继续验证”。据此允许形成**候选D：四线程／三步／完整原参考**，仅变更numSteps及对应日志，保持分段、预生成、自然语速合成和播放器倍率不变。该反馈只覆盖给出的桌面短样本，不替代任意正文清晰度或Android长听。下一步复用现有实引擎1.45×发声和三项听书回归，记录实际声音；通过后再约真机窗口，期间不占用手机。

## 候选D电脑验收与等待真机

- 产品diff仅两行：生成步数4→3及对应启动日志；没有调度、并发或生命周期改动。独立检查确认JNI直接传递正整数，现有extra未覆盖步数，回调签名及取消检查不依赖步数；未发现需要修复的问题。四步官方建议与短样本听感边界保留。
- 同一最终源码 `assembleDebug assembleDebugAndroidTest` 成功。专用StoryReader_API34／Pixel 8／ARM64模拟器三项既有实引擎回归116.276秒通过：1×跨章熄屏和通知操作、2×暂停／停止／页顶重开及定时选择、合成时失焦保留声音至焦点恢复；不将该短2×路径推广为热态供给已通过。此前未受影响的定时、存储及数字规范化回归继续继承。
- 1.45×既有实声入口通过；有效音量下补录的一次9.846秒测试记录自然PCM6688ms、合成2370ms、提交到首声2444ms（不含初始化2862ms）、播放回调跨度4523ms。实际输出约4.627秒，末尾2.900秒为播放完毕后的空闲，单句内部无≥0.6秒静音。首轮旧媒体音量命令失效，其录音不作为声音通过证据；补录前已用有效命令固定音量7。
- scrcpy重新下载及官方API入口TLS超时，未改用户网络。复用模拟器内置WAV音频后端取声，无新录音程序。原始录音在低媒体音量下峰值−36.5dB，另存固定+30dB试听副本（峰值−6.5dB，无削波）并在副本按−35dB检查。该后端会省略部分设备空闲时间，**不能用整段WAV的静音统计代替跨段墙钟连续性，也没有本轮Android逐字转写结论**；下一真机长测仍需连续时间轴的实际音频。这是声音证据范围，不把逻辑回调充当长听通过。
- [候选D APK](/Users/longshengxi/proj/story_app/app/build/outputs/thermal-investigation/daliamao-reader-thermal-candidate-d-20260923.apk)：224,970,992字节，SHA256 `6522668d9c1598ea65676170781603a521ba0694baaa6b5c36e17e691eba7abd`；签名校验通过，证书SHA256与原包相同。仅覆盖安装在模拟器，未操作手机；不是已解决发热的交付版。原问题包与已撤回C继续保留各自历史身份。

证据归入 `evidence/candidate-d-emulator-20260923/`；模拟器已关闭。本阶段临时目录、失败下载、生成源码及未采用音频清理，未引入一次性验收系统；累计仍按约770行保守预算维护。按用户之前明确要求，电脑准备完成后暂停目标，等待重新约定约25–30分钟的真机窗口。下一步先取得可用的连续实声录制工具，保存手机当时最新数据，再按原定1.45×亮屏不充电20分钟场景和既定改善门槛测候选D；记录实际初温，不拘泥精确数字、不用温差较小代替改善。若未达标，不因已接受短样本听感而宣称成功。

## D首次约定窗口未执行（录音依赖未备妥）

16:13用户连接USB并给出25分钟窗口。确认仍为15 Ultra，初始电池32.3°C、53%、USB供电；只保存本次最新三份设置／进度文件，没有安装D、写入测试设置或开始长听。此前已知scrcpy下载故障没有在约窗口前解决，本次官方URL直连及当前7897代理仍TLS握手超时，故停止占用手机，正常打开原阅读页并明确交还；本次备份已删除，后续仍须重新备份用户最新数据。没有新的温升、耗电或声音验收结果。

电脑侧确认部分HTTPS站点可达，但GitHub、官方认可的ghcr分发域及所试中转均未取到工具；单次使用公开DNS/IP也未解决GitHub TLS握手。未修改用户系统代理或DNS。官方静态macOS ARM64 4.1包校验值为 `20fd47c9014dd5e0fa77091f3cb7adbda8445a360c4584aeaa0150b5b3988ff3`，见[固定版本官方文档](https://github.com/Genymobile/scrcpy/blob/v4.1/doc/macos.md)。后续任何替代传输须先核对完整摘要，工具实际可用后才重新约手机；不继续在用户留出的窗口里排查下载。已集中询问电脑可用网络／浏览器访问条件，等待补充。本次不新增验收程序，沿用约770行累计预算。

用户反馈浏览器似乎也无法打开GitHub，并指出录音工具应可复用。此前可工作的scrcpy包被放在本任务临时目录，阶段清理时一并删除；这不是录音需要联网，而是工具管理失误。恢复下载后应将校验过的4.1工具放入固定开发工具位置，保留版本／来源／摘要，离线复用；仅清理一次性测试脚本、备份及中间材料。不把重新下载当作每轮测量的步骤，也不在安装和实际录音预检完成前再占用真机窗口。此时工具仍未恢复，没有增加真机验收结果。

## 录音依赖恢复

清华Homebrew镜像成功取得4.1_1 ARM64 Sonoma包，SHA256 `3138af17805ee58bf974e234863402c5f4e829004f6113b80e52ee39c011ae32` 与官方API一致；包内server与上游4.1摘要一致。该客户端依赖本机尚未安装的FFmpeg 9，未因此升级现有工具链。通过Git SSH取得上游v4.1（`2926c06c5dc3064ae6d8db706f1a98a37cfcf3f0`），未改源码，以本机FFmpeg 8.1.2／SDL 3.4.10构建客户端，复用已校验server；固定安装路径与维护边界见[环境操作说明](../../knowledge/gradle-android-environment-troubleshooting.md)。客户端SHA256 `79ed229e1855b54aed64f46963b3a6b90aba71c5233b413be4388f2c4e9552b7`。这不是旧静态客户端的同一二进制，但手机端版本、组件及录音参数相同。

在专用API34模拟器媒体音量0下，用基线playback／无投屏／无电脑播放参数预检：既有1.45×单句测试10.256秒通过，录得39.452秒Opus连续时间轴，含约4.47秒实际声音及前后空闲，峰值−1.4dB。该结果只确认录音工具可用，不新增真机长听或逐字正确性结论。证据保存在`evidence/recorder-restored-20260923/`，模拟器关闭；不可用的Homebrew客户端已卸载，固定工具与libusb保留，后续录音无需重新下载。未新增验收程序。

## 候选D第一次真机20分钟：温度达标，耗电门槛待证

用户重新授权25–30分钟窗口。16:43:31–17:03:31（设备标记epoch `1790153011.087`–`1790154211.064`），15 Ultra、同书同源chapter=3／offset=987、1.45×、亮屏、系统亮度41／应用0.8、不定时、持续不充电。仍为同一候选D APK；日志确认为四线程／三步，UI显示1.45×。安装前独立新备份三份设置／位置文件；本次不等待精确初温。起始SOC51%高于基线45%，室温未测，保留普通后台应用，这些差异限制因果和绝对节电量解释。

| 指标 | 四步基线A | 三步候选D首次 | 判断 |
| --- | --- | --- | --- |
| 电池服务起点→20分钟读数 | 31.6→42.7°C | 33.9→40.4°C | 首轮终点温度门槛通过；D实时HAL电池终点40.9°C，不能混成同一读数 |
| skin传感器起点→终点 | 32.756→约46.5°C | 34.761→43.851°C | 均是系统传感器／估计量，不是外壳接触测温 |
| 第15–20分钟skin均值 | 45.853°C | 43.341°C（11点） | 下降约2.51°C，通过预定至少1.5°C门槛；仍需方向重复 |
| 电荷计20分钟首尾 | 2286→1830mAh，减少456mAh | 2583→2234mAh，表面减少349mAh | D末2分钟未更新，不能按该值声称节电约23% |
| 暂停后的电荷补更新 | 不用异条件尾段追加到基线 | 停播约1分钟读到2188mAh，累计减少395mAh | 比20分钟终值又减少46mAh；含空闲尾段且有滞后，不能直接当等窗口精确耗电。预定≤387.6mAh／至少15%门槛尚未可靠证明 |
| 用户外壳手感 | 明显发烫 | “温热，较上次明显减轻” | 体感反馈，与传感器分开记录 |

完整实录中，从首声至20分钟主动暂停没有≥0.6秒内部静音（阈值−35dB），旧基线的一处1.765秒停顿在本窗口未再出现。首声约5.0秒，未见超过原约5.8秒＋1秒的退化；该数为录音时间轴与播放日志对齐的近似值，不单据回调声称出声。274个正式窗口内完成段累计合成677.579秒、自然音频1730.848秒；末5分钟67个完成段的1.45×播放时长预算／合成耗时为1.550。这个合计支持有供给余量，不能当作逐段最小余量、CPU降幅或能耗占比；没有把不同正文集合直接做百分比配对。没有本轮Android逐字转写结论，短样本听感接受和未改文字流程的旧回归仍按原范围继承。

到点媒体暂停后没有持续新合成，两次进程CPU快照均0%，当时Wake Locks为0。随后单独检查热态熄屏恢复，12秒实际音轨持续出声，无≥0.6秒内部静音；它不属于20分钟温耗窗口，也不证明整夜后台。停止后服务消失、进程CPU仍0%；停止时系统另有唤醒锁，不能称全机唤醒锁为0。恢复本次最新备份时三份SHA逐一相同，包含用户后来阅读的另一部小说chapter34／offset7268；系统自动亮度、原亮度42和60秒息屏恢复，原小说正常打开，无线ADB关闭，手机交还。手机保留D供正常使用，明确仍为待复测候选，不是已通过完整验收的最终版本。

结论：减少Decoder生成步数已取得首轮真机降温、连续播放及用户体感的同方向证据，不再仅凭电脑CPU筛选。**尚未完成可重复验收，尤其不能宣称节电≥15%或“发烫已解决”。** 下一窗口在手机自然冷却、方便使用时再约一次同条件20分钟；先核对可用的更及时电荷读数，记录正式终点及停播后补更新，不能用计数滞后或空闲尾段事后修正成通过。若现有读数仍无法跨过门槛和波动，报告耗电未达证并提出保留降温收益的取舍，不无限调参。17 Pro、其他语速与D的热态2×仍未验收。

对以上结论做了一次有边界的独立只读核对，确认温度数值达标与完整验收应区分；349mAh不能支持精确节电比例，395mAh也包含空闲尾段，既不能直接改判通过，也不能断言真实20分钟必然失败。未发现需要改实现的问题，检查至此收口。

本轮新增既有命令采样13行、临时温度CSV解析14行、两条简单汇总共2行；连同此前保守约770行，全任务约799行，已到停止新增验收代码的边界。后续仅复用已有入口／标准工具，若确需新代码必须先明确预算问题。原始必要证据归入`evidence/candidate-d1-20260923/`，UI转储、用户备份及重复中间材料在本阶段清理；目标尚未完成，仅为下一次授权复测保留同一13行采样命令及14行解析入口于原临时目录，目标结束时删除，不进入仓库。固定scrcpy及其源码／许可证保留。

## 接受轻微听感取舍后的两步桌面筛选

用户提供网页端讨论作为参考，明确不是要求照单实施，并允许轻微效果损失换取较低生成成本；先做有价值的本地尝试，手机仍等约定窗口。短参考、三线程、线程空转和真正合块已有证据，沿用其范围，不把外部建议当作新发现。参考前处理缓存仍有机制上的机会，但现有证据没有将其列为主要成本；电脑预生成可将计算移出手机，却增加准备与同步流程，当前不实现该新功能。任意跳过网络层也不是现有模型公开支持的运行参数，不优先改图。

本次只比较三步与两步，保持四线程、完整雷军参考、自然speed=1、silenceScale=0.2、原生min30。两个既有代表摘句按3→2→2→3固定顺序运行，覆盖叙述、中文日期时间、人名和短尾。先要求同文累计生成CPU与单位音频CPU均至少下降15%、最终时长不系统性变慢且变化不超过5%，再展示预定第一轮声音供听感判断。轻微听感许可不等于接受漏读、重复或静默改低播放倍率；若有希望且用户接受样本，再决定是否形成手机候选，否则保留D。只做本组，不扩参数矩阵或循环试层数；手机验收门槛不变。

原拟用官方CLI以避免扩写验收代码；固定版Python示例仍写死参数，C++工具下载TLS超时，停止下载。用户随后明确允许过期验收代码回收额度，需要更多时可以申请。按此新授权，只回收已删除的28行桌面生成入口和31行短参考变体，共59行；现重新使用本机sherpa 1.13.8与原模型，临时入口40行，累计占用约780／800。生成计时从调用前至返回后，不含引擎初始化和WAV写入，与旧桌面口径一致；以上决定均在本次生成前确定。其余已删除历史代码暂不另行释放，也不更改跨任务稳定规则。

本组已完成：三步累计CPU25.572秒、两步17.173秒，减少32.85%；两者最终自然音频均27.371秒，因此单位音频CPU同幅下降，两个两步轮次均有收益。各摘句最终长度也相同；这不证明没有漏字或音色退化。三步每轮两句合成墙钟3.405／3.366秒，两步2.174／2.189秒。仅支持该桌面模型／输入下减少计算，不转换为手机节电比例，也不宣称找到所有输入的最佳步数。

按事先选择使用第一轮两段原始声音，均经FFmpeg atempo=1.45输出同文试听，向用户询问音色／清晰度／自然度是否可接受；该处理不是Android AudioTrack录音。两步仍低于官方列出的四步范围，需要实际听感取舍。暂未修改产品、构建新APK或操作手机，D及其真机证据保持有效；等待用户试听后，再决定是否形成两步候选或继续复测D。必要JSON和原始／试听WAV归入`evidence/desktop-two-steps-20260923/`，不入Git。新增两行数值汇总后预算暂按约782／800记；阶段清理删除这次40行入口后按用户授权释放，余额占用约742／800。

两个试听文件均约9.45秒，平均音量三步−20.0dB、两步−22.5dB，峰值分别−0.3／−3.0dB；向用户明确两步更轻，避免将响度差直接当作清晰度差。没有做响度补偿或宣称独立逐字听验通过。

## 候选E：先单独验证两步（后因较长听感反馈撤回）

用户反馈“两步的其实还可以”，并询问是否值得混合其他方法。接受该短样本不等于任意正文和长听质量已过关。下一主候选E只改四线程／完整参考下的numSteps=2及日志，不叠加短参考、合块、线程或缓存改造。理由是本项直接减少主要解码阶段计算、同文桌面收益明确，且尚未测其真机温耗，先取得这一项结果可以决定是否还需要额外代价。短参考是有计算收益依据的备选，但旧样本存在节奏变化；两步下线程余量改变，旧四步三线程失败不能证明新组合必然失败，也不能推定该组合更省电。两项仅在E不足时再按剩余瓶颈选择一项，不同时铺开。

本地准备复用现有实引擎发声与听书控制回归，录制实际声音；步数是纯推理配置变更，不新增镜像参数值的测试，无并发／生命周期改动，不重开独立实现审查。保留D和原问题APK，待本地验证完成后再约真机1.45×窗口；不自动连接或安装到手机。本项一次性验收代码预计0行，按用户允许过期回收后的约742／800继续管理。

用户补充目标是最终组合的温耗改善，不要求精确归因每项贡献。据此在已完成两步构建后，补一个便宜的电脑组合筛选：两步＋此前1.930秒参考片段，对照两步＋完整参考，其他配置不变，同两个摘句按完整→短→短→完整运行。不假设旧四步收益可以直接与减步收益相乘，也不因旧短参考时长门槛失败而断言两步结果。组合先要求额外生成CPU至少少15%、各段及合计自然时长变化不超过5%，有希望再请用户听音色／完整性；若仍超出时长门槛，本轮不继续调截点，交付单独两步候选待真机。只新增此项，不要求分别给所有组合占手机窗口。预计临时入口约45行，预算仍在800以内，结束清理后按新授权回收。

组合小样本CPU19.344→12.539秒（−35.18%），两次同方向；但叙述段6.688→约6.219秒（−7%），正文短尾段6.997→7.488秒（＋7%）。没有通过预设节奏门槛，不自动放宽标准，也没有将组合写入App。考虑用户已允许轻微效果损失，展示声音请求明确取舍；用户答复短样本不足以下结论，要求更长、含对话及数字，并加原四步对照。组合结果保存在`evidence/desktop-two-short-reference-20260923/`。

E本地构建与既有回归已完成：`assembleDebug assembleDebugAndroidTest`成功；1.45×实引擎单句9.416秒通过，初始化2924ms、合成1870ms／自然6688ms、首播1975ms（不含初始化）、播放跨度4498ms。该次scrcpy录制24.62秒，首声在20.591秒出现，录制到点早于整句结束，**只能作为约4秒实际输出片段，不能记为全句声音验收**；不为对话的新试听要求重复跑这条逻辑测试。另两项实引擎回归68.121秒通过：1×短尾跨章熄屏／通知暂停恢复，以及合成期间失焦保持音频、焦点回来不重复生成。未改动的旧存储／定时等证据继承；E尚缺1.45×真实小说跨段完整实录及真机长听，不能称已具备最终验收。证据保存在`evidence/candidate-e-emulator-20260923/`，模拟器关闭，不操作手机。

E APK已独立保留为`daliamao-reader-thermal-candidate-e-20260923.apk`，225,249,411字节，SHA256 `27512be6cff5bac963f78d14b121124cbf841a26158f3c6d335544a9e59a661f`。签名验证通过，证书SHA256仍为`cba64d3b44548055d79ff4e677e64d7f464cdf59fbaf538cf41a377dfc1912eb`，可以覆盖安装但尚未安装手机。手机仍是D；E只改两处步数及日志，没有更换资源／参考音色或改并发调度。

## 较长同文试听，等待用户与其他试听者判断

按用户新要求编写8段代表文字，含叙述、人物对话、姓名、年份、时刻、小数及百分比；不是用户小说摘录。数字预先写成现有规范化器对应的中文输入，各段约30–40字，三种配置输入及分段完全相同。固定sherpa 1.13.8、4线程、speed=1、silenceScale=0.2及min30，各生成一次：A四步完整参考、B两步完整参考、C两步1.930秒参考。该组是听感样本，不补做性能统计；不将主观比较包装为新节电证据。

统一FFmpeg atempo=1.45后，A／B／C分别36.21／36.83／35.42秒。再仅作固定衰减，将平均音量统一约−24.5dBFS，避免原响度差干扰比较；峰值分别−4.6／−3.6／−3.1dBFS，没有削波、动态压缩、降噪、修音或单句修补，也没有额外调速对齐长度。原生遇到单独引号的OOV日志与旧机制一致，各正文段均产生非空音频；这不证明逐字无漏读，质量等用户实听。自然原音、24条输出记录与实际合成文字保存在`evidence/long-quality-20260923/`。

交付目录`app/build/outputs/thermal-investigation/listening-comparison-20260923/`包含三份WAV、带数字的原文和说明，另提供同名ZIP便于转发。仅供电脑听感选择，不是Android播放连续性或温耗验收。当时按用户要求备妥后暂停，等待用户及其他试听者判断；后续已收到两步不清楚的反馈并撤回E，见下节。试听C指两步短参考，不是旧三线程候选C。

本轮新增短参考入口40行及2行数值汇总，入口删除后释放；较长试听入口41行，结束同样删除并释放。按上轮约742行占用保守记，本轮峰值约785、结束约744／800，未引入验收平台或新仓库测试工具。临时目录、源码与重复声音清理，必要证据及可转发样本留在既有忽略产物目录，固定录音工具继续保留。

## 撤回两步，补三步完整／短参考试听后暂停

用户较长试听反馈“两步会有那种电视滋滋噪声的几个字的声音出来，就听不太清楚”，并认为短参考影响似乎不大，要求先制作三步＋短参考再暂停。记录为用户主观质量反馈，未定位具体秒数、噪声机制或证明所有两步输入均失败；但已足够撤回E作为下一手机候选，不用CPU收益覆盖清晰度问题。源码仅把numSteps及日志2恢复为3，`git diff 11b43d6 -- app/src/main`为空，恢复已测D实现并继承其有效回归，不为原样回退重构或重复构建。E的历史APK、测试与声音证据保留，不再推荐安装。手机始终留D，本轮未操作设备。

复用上一轮完全相同8段对话／数字文本、分段、模型及运行库，生成三步完整参考与三步1.930秒参考各一份。统一1.45×后分别36.71／35.94秒，整体固定衰减至与旧样本相同的约−24.5dBFS，不降噪、修音或逐句修补，也不强行对齐时长。本轮只为听感选择，不采集／宣称新的CPU降幅；短参考此前的四步／两步CPU收益不能直接套到三步。原先短参考的节奏门槛失败仍保留，是否接受新组合需听实际声音，不因完整片段总时长接近就宣称各句无变化或无漏读。

`listening-three-steps-20260923/`包含两个新WAV、原四步同文WAV（直接复用）、原文及说明，并提供同名ZIP。自然原音、16条生成记录及输入保存在`evidence/three-reference-quality-20260923/`。新临时入口41行，约744→785／800，清理后按用户授权释放回约744；无新验收平台。备妥后按用户要求暂停目标，等待听感回复，不推进短参考产品化或新真机窗口。
